

const Home = ({ posts}) => {
  return (
    <main className="Home">
      
      {/* <h1>Home</h1>
      <ul>
        {posts.map(post => (
          <li key={post.id}>
            <Link to={`/post/${post.id}`}>{post.title}</Link>
          </li>
        ))}
      </ul>  */}
    </main>
  )
}

export default Home
